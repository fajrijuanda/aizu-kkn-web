@props(['title' => 'Belum ada data', 'description' => 'Konten akan ditampilkan setelah data tersedia.'])

<div class="rounded-2xl border border-dashed border-green-200 bg-green-50 p-8 text-center">
    <p class="text-sm font-semibold text-green-950">{{ $title }}</p>
    <p class="mt-2 text-sm text-green-700">{{ $description }}</p>
</div>
